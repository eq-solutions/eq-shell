// Lazy-loaded stub for the Cards module. Replace with real
// implementation in Phase 2+. The default-export shape is the
// React.lazy() contract.

import ComingSoon from '../pages/ComingSoon';

export default function CardsModule() {
  return <ComingSoon module="Cards" />;
}
