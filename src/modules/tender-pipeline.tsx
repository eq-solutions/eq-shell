// Lazy-loaded stub for the Tender Pipeline module.
// Phase 2 will replace this with the full React port — see the
// Phase 2 spike PR for the migration plan / line ranges in
// eq-solves-field/scripts/tender-pipeline.js.

import ComingSoon from '../pages/ComingSoon';

export default function TenderPipelineModule() {
  return <ComingSoon module="Tender Pipeline" />;
}
