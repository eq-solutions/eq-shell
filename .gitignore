# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local environment
.env
.env.local
.env.*.local

# Netlify CLI scratch
.netlify/

# Deno runtime lockfile (netlify-cli edge-functions bootstrap)
deno.lock

# Dev preview artifacts
.claude/

# eq-tokens CDN build outputs (vendored from eq-platform packages —
# these are generated by `pnpm build` in the vendor; tracking them in
# the consumer repo is noise. Accidentally added 2026-05-21).
packages/eq-tokens/cdn/

# Auth email-template backups (production config snapshots — restore-only, not source)
scripts/auth-templates/backup-*.json
